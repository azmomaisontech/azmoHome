import * as React from "react";
import NavBar from "../component/NavBar";
import Header from "../component/Header";

const Home: React.FC = () => {
  return (
    <React.Fragment>
      <NavBar />
      <Header />
    </React.Fragment>
  );
};

export default Home;
